apiVersion: v1
kind: Service
metadata:
  name: frontend
  labels:
    app.kubernetes.io/name: gmp-frontend
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: gmp-frontend
  ports:
    - name: web
      port: 9090
      targetPort: web
