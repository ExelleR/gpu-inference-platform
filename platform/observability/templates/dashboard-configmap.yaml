{{- if .Values.dashboards.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-dashboard-vllm
  labels:
    grafana_dashboard: "1"
data:
  vllm.json: |
{{ .Files.Get "dashboards/vllm.json" | indent 4 }}
{{- end }}
